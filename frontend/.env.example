# The frontend has no identity-provider secrets or build-time authentication settings.
# Authentication options are loaded from the same-origin /api/auth/providers endpoint.
# Configure AUTH_* and optional OIDC_* in the backend environment.
